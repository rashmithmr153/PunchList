<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- PALETTE: SINGLE-HUE MINIMAL (Off-black, muted slate blue accent) -->
    <Color x:Key="ColorBackground">#111113</Color>
    <Color x:Key="ColorSurface">#1A1A1D</Color>
    <Color x:Key="ColorSurfaceHover">#26262C</Color>
    <Color x:Key="ColorSurfaceActive">#30313A</Color>
    <Color x:Key="ColorBorder">#2A2A2E</Color>
    <Color x:Key="ColorBorderHover">#42424A</Color>
    <Color x:Key="ColorTextPrimary">#F2F2F3</Color>
    <Color x:Key="ColorTextMuted">#A0A0A6</Color>
    <Color x:Key="ColorAccent">#6E7AA8</Color>
    <Color x:Key="ColorAccentHover">#828EB8</Color>
    <Color x:Key="ColorAccentSubtle">#1A1E29</Color>

    <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColorBackground}" />
    <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}" />
    <SolidColorBrush x:Key="BrushSurfaceHover" Color="{StaticResource ColorSurfaceHover}" />
    <SolidColorBrush x:Key="BrushSurfaceActive" Color="{StaticResource ColorSurfaceActive}" />
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}" />
    <SolidColorBrush x:Key="BrushBorderHover" Color="{StaticResource ColorBorderHover}" />
    <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource ColorTextPrimary}" />
    <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}" />
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}" />
    <SolidColorBrush x:Key="BrushAccentHover" Color="{StaticResource ColorAccentHover}" />
    <SolidColorBrush x:Key="BrushAccentSubtle" Color="{StaticResource ColorAccentSubtle}" />

    <!-- TYPOGRAPHY: Segoe UI Variable with fallbacks -->
    <FontFamily x:Key="FontBody">Segoe UI Variable Text, Segoe UI, Inter, sans-serif</FontFamily>
    <FontFamily x:Key="FontDisplay">Segoe UI Variable Display, Segoe UI, Inter, sans-serif</FontFamily>

    <!-- UNIFIED OUTLINE ICON GEOMETRIES (24x24 viewBox, stroke-width=1.5 / 2 equivalent) -->
    <!-- 1. Add -->
    <StreamGeometry x:Key="GeoAdd">M 12,5 L 12,19 M 5,12 L 19,12</StreamGeometry>
    <!-- 2. Check / Complete -->
    <StreamGeometry x:Key="GeoCheck">M 4,12 L 9,17 L 20,6</StreamGeometry>
    <!-- 3. Clock / Timer -->
    <StreamGeometry x:Key="GeoClock">M 12,2 A 10,10 0 1,0 22,12 A 10,10 0 0,0 12,2 Z M 12,6 L 12,12 L 16,14</StreamGeometry>
    <!-- 4. Snooze / Clock-Arrow -->
    <StreamGeometry x:Key="GeoSnooze">M 12,3 A 9,9 0 1,0 21,12 M 12,7 L 12,12 L 15,15 M 22,10 L 22,5 L 17,5</StreamGeometry>
    <!-- 5. Tray / Pin -->
    <StreamGeometry x:Key="GeoPin">M 12,17 L 12,22 M 5,17 L 19,17 M 8,2 L 16,2 M 9,2 L 9,9 L 6,13 L 6,17 L 18,17 L 18,13 L 15,9 L 15,2</StreamGeometry>
    <!-- 6. Settings (Gear) -->
    <StreamGeometry x:Key="GeoSettings">M 12.22,2 H 11.78 A 2,2 0 0,0 9.8,3.58 L 9.5,4.7 A 2,2 0 0,1 7.3,6.1 L 6.1,5.6 A 2,2 0 0,0 3.7,6.8 L 3.5,7.2 A 2,2 0 0,0 4,9.6 L 4.9,10.4 A 2,2 0 0,1 4.9,13.6 L 4,14.4 A 2,2 0 0,0 3.5,16.8 L 3.7,17.2 A 2,2 0 0,0 6.1,18.4 L 7.3,17.9 A 2,2 0 0,1 9.5,19.3 L 9.8,20.42 A 2,2 0 0,0 11.78,22 H 12.22 A 2,2 0 0,0 14.2,20.42 L 14.5,19.3 A 2,2 0 0,1 16.7,17.9 L 17.9,18.4 A 2,2 0 0,0 20.3,17.2 L 20.5,16.8 A 2,2 0 0,0 20,14.4 L 19.1,13.6 A 2,2 0 0,1 19.1,10.4 L 20,9.6 A 2,2 0 0,0 20.5,7.2 L 20.3,6.8 A 2,2 0 0,0 17.9,5.6 L 16.7,6.1 A 2,2 0 0,1 14.5,4.7 L 14.2,3.58 A 2,2 0 0,0 12.22,2 Z M 12,9 A 3,3 0 1,0 12,15 A 3,3 0 0,0 12,9 Z</StreamGeometry>
    <!-- 7. History / Archive -->
    <StreamGeometry x:Key="GeoHistory">M 3,12 A 9,9 0 1,0 6,5.3 L 3,8 M 3,3 L 3,8 L 8,8 M 12,7 L 12,12 L 15,14</StreamGeometry>
    <!-- 8. Trash / Delete -->
    <StreamGeometry x:Key="GeoTrash">M 3,6 L 21,6 M 19,6 L 19,20 A 2,2 0 0,1 17,22 L 7,22 A 2,2 0 0,1 5,20 L 5,6 M 8,6 L 8,3 A 1,1 0 0,1 9,2 L 15,2 A 1,1 0 0,1 16,3 L 16,6 M 10,11 L 10,17 M 14,11 L 14,17</StreamGeometry>
    <!-- 9. Close (X) -->
    <StreamGeometry x:Key="GeoClose">M 18,6 L 6,18 M 6,6 L 18,18</StreamGeometry>
    <!-- 10. Refresh -->
    <StreamGeometry x:Key="GeoRefresh">M 23,4 L 23,10 L 17,10 M 1,20 L 1,14 L 7,14 M 3.51,9 A 9,9 0 0,1 18.36,5.64 L 23,10 M 1,14 L 5.64,18.36 A 9,9 0 0,0 20.49,15</StreamGeometry>

    <!-- UNIFIED OUTLINE ICON STYLE (Consistent 14x14 dimensions, 1.5 stroke, round caps/joins) -->
    <Style x:Key="PathIcon" TargetType="Path">
        <Setter Property="Width" Value="14" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="StrokeThickness" Value="1.5" />
        <Setter Property="StrokeStartLineCap" Value="Round" />
        <Setter Property="StrokeEndLineCap" Value="Round" />
        <Setter Property="StrokeLineJoin" Value="Round" />
        <Setter Property="Stroke" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Control}}" />
    </Style>

    <!-- MINIMAL DARK CONTEXT MENU STYLE (Removes white icon gutter completely) -->
    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="Background" Value="{StaticResource BrushSurface}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter ContentSource="Header" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#252A38" />
                            <Setter Property="Foreground" Value="#FFFFFF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TraySeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource BrushBorder}" />
        <Setter Property="Margin" Value="4,4" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}" Height="1" SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- CONVERTER -->
    <BooleanToVisibilityConverter x:Key="BoolToVis" />

</ResourceDictionary>
